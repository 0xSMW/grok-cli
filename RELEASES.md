# Releases

This repository is now the Rust port of the Grok CLI. Historical release notes imported from the source repository were removed because they describe the old implementation, not current Rust release history.

## Unreleased

- Rust workspace bootstrap is in progress around `grok-client`, `grok-cli`, and `grok-proxy`.
- No Rust-port release entry is recorded yet.
- Future entries should describe Rust crate and CLI changes only, with parity notes tied to current Cargo-based behavior.
